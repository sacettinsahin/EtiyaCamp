import { ClickProductsDirective } from './click-products.directive';

describe('ClickProductsDirective', () => {
  it('should create an instance', () => {
    const directive = new ClickProductsDirective();
    expect(directive).toBeTruthy();
  });
});
